
NODE_ENV="your development environment"
DATABASE_URL="database url, example: postgresql://nuru:[db-password]@localhost:5432/db_name"
SESSION_SECRET="your auth session secret"

ADMIN_PASSWORD="initial admin password"
ADMIN_FULLNAME="initial admin fullname"
ADMIN_PHONE="initial admin phone number"
ADMIN_EMAIL="initial admin email"
ADMIN_SEED_ENABLED="a boolean for whether seed or not"

NEXT_PUBLIC_BASE_URL="site url"

# Optional: Google Search Console verification token (adds the meta tag site-wide)
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION="your google site verification token"

CLOUDINARY_CLOUD_NAME="your cloudinary name"
CLOUDINARY_API_KEY="your cloudinary api key"
CLOUDINARY_API_SECRET="your api secret"

NEXT_PUBLIC_TINYMCE_API_KEY="your tiny mce api key here"

UPSTASH_REDIS_REST_URL='your apstash redis url'
UPSTASH_REDIS_REST_TOKEN='your upstash redis rest token'

# Gmail SMTP — used to email two-factor authentication (2FA) codes.
# GMAIL_PASSWORD must be a Gmail App Password (not your account password).
GMAIL_USER="your gmail address"
GMAIL_PASSWORD="your gmail app password"
EMAIL_FROM_NAME="Chosen Fintech Solutions"

# Optional: where public contact-form enquiries are delivered.
# Falls back to ADMIN_EMAIL when unset.
CONTACT_RECIPIENT_EMAIL="inbox that receives contact form messages"


